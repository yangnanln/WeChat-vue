<!--components/moveItem/index.wxml-->
<view class="container"  bind:tap="hanleDetail">
    <image class="" src="{{movie.imageUrl}}" > </image>
    <text >
        {{movie.title}}
    </text>
    <view >评分<text >{{movie.average}}</text>
    </view>
</view>
